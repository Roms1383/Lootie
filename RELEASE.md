# release notes

- bump:
  1. `Plugin::VERSION` in *.\src\lib.rs*
  2. workspace version in *.\Cargo.toml*
- tag accordingly

## after patch update

- SDK: update `red4ext-rs` ➡️ `RED4ext.SDK`
